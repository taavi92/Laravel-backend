1) if run in localhost need to change " .env" file
DB_CONNECTION=
DB_HOST=
DB_PORT=
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=


2) "php artisan migrate"  to created words table
3) ready to start enviroment " php artisan serve"

